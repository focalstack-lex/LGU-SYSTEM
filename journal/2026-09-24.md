# Journal — 2026-09-24

## Adoption proposal document (new deliverable)

- [15:05] **Created** `Adoption Proposal - Financial Transparency and Student Academic Portal.docx` (repo root) plus a rendered `…pdf` companion — a formal institutional-adoption proposal for a school wishing to deploy this portal. Built with the docx skill (Report/Proposal scene Template F, Profile A / Times New Roman, cover recipe R1 + GO-1 Graphite Orange palette).
  **Contents (40 pages, ~9,500 words, 22 tables, 4 verified product screenshots):** cover; letter of transmittal; 2-page Table of Contents (52 headings, field-driven); 10 body chapters (executive summary; problem analysis; objectives; solution description incl. a security-controls table disclosing four limitations; five-phase roadmap; resources and indicative cost structure; risk register; benefits and a KPI evaluation framework; governance/data ownership; recommendation and request for approval); 7 appendices (role & permission matrix, module inventory, institutional configuration checklist, technical requirements, known limitations, glossary, review & acceptance with signature blocks).
- [15:26] **Verified** (full gate, in order): `postcheck.py` **9/9 checks pass, 0 errors, 0 warnings**; `add_toc_placeholders.py --auto` **exit 0**, 52 TOC entries injected and `updateFields=true` set; `fix_footer_fields.py` applied Roman (`PAGE \* ROMAN`) to front matter and Arabic (`PAGE \* arabic`) to body, and removed the empty cover `pgNumType`. Rendered DOCX→PDF via LibreOffice 26.8.0 and inspected all pages: page numbering verified as **I, II, III** (front matter) then **1–36** (body), and a scripted scan found **no blank or near-empty pages**.
- [15:20] **Fixed four layout defects found by inspecting the rendered pages** (visual-judge subagent was unavailable — provider error — so the pages were inspected directly, per the skill's fallback): (1) the transmittal letter's signature block was stranded on a near-empty page 3 → letter trimmed and the closing/signature block bound with `keepNext`, now one page; (2) an orphan page holding only Figure 4 → the figure was moved into §4.2 alongside the other financial screenshots and renumbered; (3) Appendix G split its two signature blocks across pages → table text compacted, now one page; (4) **root cause of unbalanced columns: `docx-js` was writing a placeholder `tblGrid` (`gridCol w=100` for every column) while cell widths were percentages, so the renderer ignored the specified proportions and collapsed wide columns to ~25 characters per line.** Fixed by passing an explicit `columnWidths` array (derived from the same percentages over the 8,788-twip text width). This corrected every table and reduced the document from 45 to 40 pages.
- [15:06] **Corrected three factual claims against source before delivery** (Zero Hallucination Directive): module count 32 → 41 (recounted from the Appendix B inventory); schema migrations 36 → 32 (`ls supabase/migrations/*.sql | wc -l` = 32); confirmed curriculum totals 189u/67, 204u/68, 213u/75 (`supabase/migrations/005_credit_unit_tracker.sql:7-9`), the eight-role catalog (`supabase/migrations/034_faculty_roles_and_submissions.sql:17-18`), and program names (`server/routes/units.js:19-23`). Also avoided unsupported claims: no quantified improvement figures, no anonymity claim for the feedback module, and the offline/click-driven nature of the Grizz assistant stated accurately.

## Toolchain / housekeeping

- [15:04] Installed **LibreOffice 26.8.0** from the Tsinghua TUNA mirror (357 MB, 8 min) after probing alternatives — TUNA measured ~170 KB/s vs ~180 B/s (TDF) and ~300 B/s (Rackspace). Silent install returned MSI exit 3010 (success, reboot recommended); registered `C:\Program Files\LibreOffice\program` on the user PATH via `setx`. Used for DOCX→PDF conversion and page rendering (PyMuPDF for PNG rasterisation; poppler/pdftoppm was not available).
- [15:12] **Corrected an accidental repo modification:** an earlier `npm install docx@9 image-size` resolved against the project root instead of the intended scratch directory, adding two dependencies to the tracked `package.json` and `package-lock.json`. Reverted both files with `git checkout --` and removed the 9 extraneous package directories from `node_modules` (identified deterministically by diffing `node_modules` against the restored lock file), then reinstalled `docx@9` with `--prefix` into the gitignored `scratch/proposal-build/`. Repo manifests are clean; only the intended deliverables are untracked.
- Build script retained at `scratch/proposal-build/generate.js` (gitignored) so the document can be regenerated and re-verified.

## Legal & Copyright Audit (/copyright)

- [16:21] **Executed `/copyright` Audit & Applied Login Disclosures**: Audited active project across seven legal compliance lenses (IP/Trademark, Accessibility, Data Privacy, Consumer Protection, Safety, Anti-Spam, Tax/Financial Transparency). Per strict scope directive, legal copyright notices and Privacy Policy / Terms of Service links were applied exclusively to authentication surfaces (`client/index.html:98-105`, `client/feedback/index.html:35-38`). Generated persisted compliance report at `reports/copyright/2026-09-24-1620.md` with an overall compliance score of 9.0/10.0.

## Security Gate Audit (/secure)

- [16:28] **Executed Security-First Deployment Gate Check**: Ran full 10-point security audit against active repository. 9 out of 10 checks passed (Secrets Scan, Input Validation, Auth/AuthZ & DB Trigger Failsafe, Helmet CSP Security Headers, Restricted Origin CORS, Exception Isolation, Parameterized DB queries, Release Cleanliness, Multi-tier Rate Limiting). 1 check failed (`npm audit`: 4 high, 4 moderate vulnerabilities in indirect/dev dependencies). Security Gate Outcome: **BLOCKED** until dependency remediation.

## CV Builder Redesign & Migration Integration

- [16:40] **Applied Migration 037 & Integrated CV Builder Standalone Module**:
  - **Database Migration**: Created [`supabase/migrations/037_cv_builder_fields.sql`](file:///c:/Users/User/Documents/LGU%20System/supabase/migrations/037_cv_builder_fields.sql) adding `full_name` and `education` fields to `public.student_cvs` (additive, idempotent, default values).
  - **Backend API Routes**: Updated [`server/routes/cv.js`](file:///c:/Users/User/Documents/LGU%20System/server/routes/cv.js) with post-auth per-user rate limiting (40 saves / 5 min), protocol prepending (`https://`) for bare domain URLs in `cvUrl()`, and trapped error codes `42703`/`PGRST204` mapping to `503 Service Unavailable`.
  - **Server Route Mounting**: Mounted explicit HTML routes `/cv-builder` and `/cv-verify` in [`server/index.js`](file:///c:/Users/User/Documents/LGU%20System/server/index.js) and removed unauthenticated IP write limiter from `/api/cv`.
  - **Frontend UI & Autosave**: Enhanced [`client/cv-builder.html`](file:///c:/Users/User/Documents/LGU%20System/client/cv-builder.html) and [`client/js/cv-builder.js`](file:///c:/Users/User/Documents/LGU%20System/client/js/cv-builder.js) with debounced 3-second autosave, minimum 8-second request interval, `localStorage` draft fallback, safe URL href escaping, and single-page PDF print styles. Added Career Passport link to main portal sidebar in [`client/index.html`](file:///c:/Users/User/Documents/LGU%20System/client/index.html).
  - **Verification**: Node syntax check passed on all modified backend server files with exit code 0.

## Mobile Responsiveness Remediation and Verification

- [18:02] **Applied Core Mobile Responsiveness Fixes across CSS Stylesheets**:
  - **Data Card Styling in 481px-768px Band (P0 Fix)**: Moved `.data-card`, `.data-card-header`, `.data-card-body`, `.data-card-row`, `.data-card-label`, and `.data-card-actions` inside `@media (max-width: 768px)` in `client/styles/main.css`. Verified `.data-card` at 400px and 600px computes with `#1C1C20` matte background, 1px border, 12px radius, and flex rows.
  - **Sidebar Footer 60px Icon Rail Reflow (P1 Fix)**: Reflowed `.sidebar-footer` and `.sidebar-actions` into a vertical column in `@media (max-width: 768px)` in `client/styles/main.css`. Verified `#user-pill`, `#theme-toggle-btn`, and `#logout-btn` centered at `left: 12px` to `13px` inside the 60px rail with zero off-screen overflow.
  - **Officer Executive Portal Grid Restoration (P1 Fix)**: Removed stray closing brace in `client/styles/officer.css` (formerly line 1814) and set `.of-stat { overflow: visible; }`. Verified on `officer.html` at 1280px and 1024px that `.of-form-row` renders in 2 columns (`1fr 1fr`), and at 768px in 1 column (`1fr`).
  - **iOS Safari Focus-Zoom Input Guard (P1 Fix)**: Updated `.onboarding-card .form-group input, select, .dd-trigger` across base and mobile media queries in `client/styles/main.css` to `16px`. Added `font-size: 16px` to `client/styles/feedback.css` and `client/styles/feedback-view.css`. Verified inputs at 390px, 768px, and standalone feedback pages compute to 16px.
  - **AI Assistant Floating Launcher Offsets (P1 Fix)**: Scoped elevated bottom positioning (`bottom: calc(74px + env(safe-area-inset-bottom))`) strictly to `@media (max-width: 480px)` in `client/styles/ai.css`, keeping `bottom: 1.5rem` / `2rem` on tablets and desktops.
  - **CSS Cleanup and Text Wrapping (P2 Fix)**: Removed stray closing brace at line 7679 of `client/styles/main.css`. Added `overflow-wrap: anywhere; word-break: break-word;` to `.announce-body` in `client/styles/main.css`.
  - **Verification Suite Execution**: Ran `node scripts/verify-mobile-claims.mjs` and `node scripts/verify-mobile-claims2.mjs` with 100% passing checks across 390px, 480px, 481px, 600px, 640px, 768px, 769px, 1024px, and 1280px viewports.




